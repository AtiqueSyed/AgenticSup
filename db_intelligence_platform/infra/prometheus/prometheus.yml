# Scrapes the otel-collector's Prometheus exporter (port 8889), which serves
# every metric the backend emits over OTLP -- see docs/TELEMETRY.md for names.

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
